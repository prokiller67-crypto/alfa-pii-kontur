# Ограничение ожидания запросов — 23 сентября 2026

Организаторский клиент ждёт ответ не более 10 секунд и разрешает повтор после
HTTP 429 с `Retry-After`. Ранее один checkout из пула PostgreSQL мог ждать
14 секунд, ещё до исполнения SQL. В контрольном внешнем прогоне на старой
версии 8 запросов превысили клиентский таймаут, 184 дали HTTP 200; точно
восстановлены 92 из 100 пар (`artifacts/attempt-14-before-http.json`).

Теперь ожидание соединения ограничено 5 секундами. Весь вызов процессора,
включая чтение, запись и повтор после разрыва Neon, имеет асинхронный бюджет
7 секунд. При истечении API закрывает обработку ответом 429 с `Retry-After: 1`,
не возвращая исходный текст. Это оставляет запас на доставку ответа, но не
гарантирует внешний SLA: холодный запуск платформы и сеть вне обработчика,
а синхронные участки не могут быть прерваны `asyncio.timeout` немедленно.
Постоянные 429 также не являются успешным прохождением нагрузки.

Три новых теста проверяют зависшее чтение, потерю подтверждения уже сохранённой
записи и отмену клиентом. После таймаута освобождается место в ограничителе
параллельных запросов. Повтор оригинала возвращает маску, а повтор этой маски
восстанавливает оригинал точно; дополнительной записи не возникает.

Локально: 90 pytest-тестов пройдены, один live-тест PostgreSQL пропущен;
Ruff и `git diff --check` пройдены. В первом общем прогоне существующий тест
плотного длинного текста получил regex-таймаут 0.1 с. Отдельный повтор теста
и повтор полного набора прошли. Детектор и порог regex в этом изменении не
менялись; чувствительность к задержкам выполнения остаётся ограничением.

## Конкурентная очистка

На настоящем Neon воспроизведена блокировка: один процесс держит блокировку
синтетической просроченной записи, а независимый запрос другого процесса
ждёт её удаления более 3 секунд. Очистка теперь использует `FOR UPDATE SKIP LOCKED`:
пропускает занятые строки и удаляет их при следующем проходе. Два live-теста
Neon после исправления пройдены, включая этот сценарий, атомарность записи,
TTL и восстановление через другой экземпляр. Обычные 90 тестов также пройдены.
Инициализация пула вынесена из обработки SQL в отдельную функцию; повтор
после разрыва соединения сохранён.

Проверка исходников №14 (версия только с бюджетом ожидания) завершилась
успешно с 9950 баллами. После её публикации короткий внешний прогон на
100 парах дал 200 ответов HTTP 200 и 100 точных восстановлений из 100,
p50 270 мс, p95 388 мс, p99 5437 мс. Это не официальный пятиминутный SLA.

## Финальная версия v3

Проверка №15 от 23 сентября, 17:18 МСК завершилась SUCCESS, **9960 баллов**,
0 BLOCKER, 3 CRITICAL, 2 MAJOR. Отчёт сохранён в
`artifacts/official-attempt-15-report.zip`. Подробностей замечаний в нём нет.

Тег `submission-resilience-v3` указывает на
`56f5cbf7082f6e418db93491527d29cfb3a6ee1c`. Все 16 файлов финального ZIP,
кроме отдельного краткого README, побайтно совпадают с файлами этой версии.
Vercel deployment `dpl_6ghb8ZMTCrdMMgwuyhWDQ9C86Tph` опубликован до отправки
попытки №15 и обслуживает `https://alfa-pii-kontur.vercel.app/process`.
После публикации получены 50 HTTP 200 и 25/25 точных циклов, p50 276 мс,
p99 5501 мс (`artifacts/attempt-15-preflight-http.json`).

Архив заявки: `artifacts/deliverables/alfa-pii-source-9960-v3.zip`, SHA-256
`1cdceb7eea311a041a6b9ae661ddcc37e37eddb5e996cfcd1f77a94af077db60`.
Презентация: `artifacts/deliverables/alfa-pii-contour-presentation-resilience-v3.pptx`,
SHA-256 `b60396b20978276113bfa89568204d4b8f76248ea0b0a332ed0bc42e6a899f7d`.
Балл выше прежнего рекорда не вырос; удалось сохранить его с исправлениями
надёжности и согласовать исходники ZIP с опубликованным API.

Итоговая форма повторно отправлена с этими ZIP, презентацией и ссылкой на тег.
После перезагрузки портал сохранил новые имена файлов, описание и VCS URL.
